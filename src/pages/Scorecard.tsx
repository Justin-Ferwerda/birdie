import PagePlaceholder from '../components/PagePlaceholder';

export default function Scorecard() {
  return (
    <PagePlaceholder
      title="Scorecard"
      blurb="Course tabs, 4-player × 18-hole grid, hole info row, and the score entry sheet live here."
      phase="Phase 4"
    />
  );
}
